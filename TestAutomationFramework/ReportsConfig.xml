<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
    <configuration>
        <!-- report theme -->
        <!-- standard, dark -->
        <theme>standard</theme>

        <!-- document encoding -->
        <!-- defaults to UTF-8 -->
        <encoding>UTF-8</encoding>

        <!-- protocol for script and stylesheets -->
        <!-- defaults to https -->
        <protocol>https</protocol>

        <!-- title of the document -->
        <documentTitle>Test Automation Reports</documentTitle>

        <!-- report name - displayed at top-nav -->
        <reportName>
            <![CDATA[
                <img src="./Logo.png"/>
            ]]>
        </reportName>

        <!-- report headline - displayed at top-nav, after reportHeadline -->
        <reportHeadline>Test Automation Report</reportHeadline>

        <!-- global date format override -->
        <!-- defaults to yyyy-MM-dd -->
        <dateFormat>yyyy-MM-dd</dateFormat>

        <!-- global time format override -->
        <!-- defaults to HH:mm:ss -->
        <timeFormat>HH:mm:ss</timeFormat>

        <!-- custom javascript -->
        <scripts>
            <![CDATA[
                $(document).ready(function() {
                $(".logo-container").html("SchneiderElectric");
                $(".report-headline").html("CPQ-Test Automation Report");
                });
            ]]>
        </scripts>

        <!-- custom styles -->
        <styles>
            <![CDATA[
            .logo-container.blue.darken-2 { background-color: #388E3C!important; font-size:12px;font-weight:bold}
            .step-details{color:blue !important;font-size:12px;font-weight:bold}
             img { border: 1px solid #f6f7fa;width: 30px; }
             .report-headline{color:white !important;font-weight:bold}
             nav{background:darkcyan}
             .side-nav{background:darkcyan}
             .test.active {background: aliceblue;font-weight: 600;}
             body.default {background: azure;}
            ]]>
        </styles>
    </configuration>
</extentreports>