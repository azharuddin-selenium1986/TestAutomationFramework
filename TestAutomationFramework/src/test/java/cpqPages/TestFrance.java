package cpqPages;
import org.testng.Assert;
import org.testng.annotations.Test;

public class TestFrance  {
	//FranceQuotation franceQuote;
	@Test(priority = 1)
	public void fillOpportunityAndQuoteInfoDetails() {
	/*	franceQuote = page.GetInstance1(FranceQuotation.class);
		franceQuote.navigateToApplication("https://google.com");
		franceQuote.setQuoteName("");
		page.GetInstance1(Opportunity_QuoteInfoPage.class).navigateToApplication("https://google.com");
		String test =page.GetInstance1(Opportunity_QuoteInfoPage.class).setQuoteName("").clickOnSPADropdown().selectSPAType("").clickOnPurchasingChannelDropDown().selectpurchasingChannel("")
		.getText();
		Assert.assertEquals(test, "Azhar");*/
	}
}
